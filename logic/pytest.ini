[pytest]
testpaths = tests
asyncio_mode = auto
filterwarnings =
    ignore::DeprecationWarning
markers =
    unit: pure unit tests with no I/O
    integration: tests that use a real database
    smoke: live-deployment HTTP probes
